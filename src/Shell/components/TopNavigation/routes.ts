import {
  ADMIN_PATH,
  HOME_PATH,
  SHOWS_PATH,
} from '../../../common/constants';

export const routes = [
  { path: HOME_PATH, label: 'Personal manager' },
  { path: SHOWS_PATH, label: 'Shows' },
  { path: ADMIN_PATH, label: 'Admin panel' },
];
